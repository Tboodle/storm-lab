# Copy to .env and adjust. Values below are the defaults from app/core/config.py.

PROJECT_NAME=storm-lab-api
API_V1_PREFIX=/api/v1

# Comma-separated origins allowed to call this API.
CORS_ORIGINS=http://localhost:3000
